# Custom Qdrant image with health check support
FROM qdrant/qdrant:latest

# Install curl for health checks
USER root
RUN apt-get update && \
    apt-get install -y curl && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

# Inherit all other configurations from base image
