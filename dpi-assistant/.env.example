# Frontend Configuration
# The base URL for the DPI backend API
# Leave empty when using Docker with nginx proxy (recommended)
# For local development without Docker: http://localhost:8080
# For production with Docker: leave empty (nginx will proxy /api/* to backend)
VITE_API_BASE_URL=

# Backend Environment Variables (for docker-compose)
# Google AI Configuration (REQUIRED)
GOOGLE_AI_API_KEY=your_google_ai_api_key_here

# AWS Configuration
AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key

# AWS Cognito Configuration
COGNITO_USER_POOL_ID=your_cognito_user_pool_id

# S3 Knowledge Base Storage (Optional - if not set, uses local filesystem)
S3_KNOWLEDGE_BASE_BUCKET=your-dpi-knowledge-base-bucket-name

# Slack Integration (Optional - for automatic knowledge base uploads)
# Get these from your Slack App settings at https://api.slack.com/apps
# Required for webhook functionality:
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_SIGNING_SECRET=your-signing-secret
SLACK_KNOWLEDGE_BASE_CHANNEL=C01234567

# Optional Slack variables (not required for webhook):
# SLACK_APP_ID=your-app-id
# SLACK_CLIENT_ID=your-client-id
# SLACK_CLIENT_SECRET=your-client-secret
# SLACK_VERIFICATION_TOKEN=your-verification-token
